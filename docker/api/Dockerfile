# TODO: Implement FastAPI Dockerfile when apps/api is built.
# Build context should be the monorepo root.

FROM python:3.13-slim

WORKDIR /app

# TODO: Copy workspace files and install with uv
# TODO: Set CMD to run uvicorn

CMD ["echo", "TODO: Implement api Dockerfile"]
