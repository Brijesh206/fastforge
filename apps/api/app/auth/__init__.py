"""Auth feature wiring for the API application.

Business logic lives in the reusable ``fastforge_auth`` package. This module
only provides the HTTP layer: dependency injection, the current-user
dependency, and thin routers.
"""
