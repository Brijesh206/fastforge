{
  "name": "@fastforge/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI components for FastForge",
  "scripts": {
    "lint": "echo TODO: eslint",
    "build": "echo TODO: build ui package"
  }
}
